import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.texam.mobile',
  appName: 'Texam Premium',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
